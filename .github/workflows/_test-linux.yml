# Linux test workflow - called by test-matrix.yml
name: Test Linux

on:
  workflow_call:
    inputs:
      config-file:
        type: string
        default: "comfy-test.toml"
      timeout-minutes:
        type: number
        default: 60
      gpu:
        type: boolean
        default: false

jobs:
  linux-test:
    runs-on: ${{ inputs.gpu && fromJSON('["self-hosted", "linux", "gpu"]') || 'ubuntu-latest' }}
    timeout-minutes: ${{ inputs.timeout-minutes }}
    env:
      PYTHONUNBUFFERED: "1"
      COMFY_TEST_GPU: ${{ inputs.gpu && '1' || '' }}
      NODE_NAME: ${{ github.event.repository.name }}
      COMFYUI_DIR: ${{ github.workspace }}/ComfyUI
      TOOLS_VENV: ${{ github.workspace }}/.tools-venv
      COMFY_VENV: ${{ github.workspace }}/.comfy-venv
      PLAYWRIGHT_BROWSERS_PATH: ${{ github.workspace }}/.playwright
    steps:
      - name: Free disk space
        if: ${{ !inputs.gpu }}
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL &
          sudo apt-get clean &
          wait

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      # --- Single cache ---
      - uses: actions/cache/restore@v4
        id: setup-cache
        with:
          path: |
            ${{ env.TOOLS_VENV }}
            ${{ env.PLAYWRIGHT_BROWSERS_PATH }}
            ${{ env.COMFYUI_DIR }}
            ${{ env.COMFY_VENV }}
          key: setup-${{ runner.os }}-v2

      # --- Setup (skip if cached) ---
      - name: Setup environment
        if: steps.setup-cache.outputs.cache-hit != 'true'
        run: |
          python -m venv $TOOLS_VENV
          $TOOLS_VENV/bin/pip install uv playwright

          git clone --depth 1 https://github.com/comfyanonymous/ComfyUI.git $COMFYUI_DIR

          python -m venv $COMFY_VENV
          $TOOLS_VENV/bin/uv pip install --python "$COMFY_VENV" -r $COMFYUI_DIR/requirements.txt

          $TOOLS_VENV/bin/playwright install chromium
          $TOOLS_VENV/bin/playwright install-deps chromium || true

      # --- Save cache ---
      - uses: actions/cache/save@v4
        if: steps.setup-cache.outputs.cache-hit != 'true'
        with:
          path: |
            ${{ env.TOOLS_VENV }}
            ${{ env.PLAYWRIGHT_BROWSERS_PATH }}
            ${{ env.COMFYUI_DIR }}
            ${{ env.COMFY_VENV }}
          key: setup-${{ runner.os }}-v2

      # --- Install comfy-test (fresh) ---
      - name: Install comfy-test
        run: $COMFY_VENV/bin/pip install comfy-test

      # --- Checkout and run ---
      - uses: actions/checkout@v4
        with:
          path: ${{ env.COMFYUI_DIR }}/custom_nodes/${{ github.event.repository.name }}

      - name: Install node dependencies
        working-directory: ${{ env.COMFYUI_DIR }}/custom_nodes/${{ env.NODE_NAME }}
        run: |
          [ -f requirements.txt ] && $TOOLS_VENV/bin/uv pip install --python "$COMFY_VENV" -r requirements.txt || true
          [ -f install.py ] && $COMFY_VENV/bin/python install.py || true

      - name: Run tests
        working-directory: ${{ env.COMFYUI_DIR }}/custom_nodes/${{ env.NODE_NAME }}
        run: $COMFY_VENV/bin/comfy-test run

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: results-linux-${{ inputs.gpu && 'gpu' || 'cpu' }}
          path: ${{ env.COMFYUI_DIR }}/custom_nodes/${{ env.NODE_NAME }}/comfy-test-results/
          if-no-files-found: ignore
          include-hidden-files: true
